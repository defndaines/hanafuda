(ns hanafuda.core
  "Hanafuda core functionaltiy."
  (:require [hanafuda.cards :as cards]))

(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))

(defn deal
  "Deal out cards for a round of play."
  []
  )
